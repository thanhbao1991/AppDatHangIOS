# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

(Bumped from v54 to v57 on 2026-09-06 after `package.json` confirmed `"expo": "^57.0.0"` — check
`package.json` again before trusting this number if it's been a while since this line was touched.)
